import { analyzeKeyword } from "../core/seoEngine.js";

export async function analyze(data, user) {
    // check usage
    // enrich data
    // call engine

    const result = analyzeKeyword(data);

    return result;
}